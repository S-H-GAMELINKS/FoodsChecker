<%= form_with(model: placetype, local: true) do |form| %>
  <% if placetype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(placetype.errors.count, "error") %> prohibited this placetype from being saved:</h2>

      <ul>
      <% placetype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
